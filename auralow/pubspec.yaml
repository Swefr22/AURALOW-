name: auralow
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
version: 1.0.0+1

environment:
  sdk: ^3.10.1

# Dependencies specify other packages that your package needs in order to work.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8
  noise_meter: ^5.1.0
  shared_preferences: ^2.5.3
  # Added from Teammate's version
  flutter_native_splash: ^2.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application.
  uses-material-design: true

  # Added from Teammate's version: Logo Asset
  assets:
    - assets/logo.png

  # To add custom fonts to your application, add a fonts section here...

# Added from Teammate's version: Splash Screen Configuration
flutter_native_splash:
  color: "#000000"   # background color (black theme)
  image: assets/logo.png
  android: true
  ios: true
  android_gravity: center

# ----------------------------------------------------------------
# FIX: Moved to the left (root level), NOT inside the splash config
# ----------------------------------------------------------------
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/logo.png"
  min_sdk_android: 21